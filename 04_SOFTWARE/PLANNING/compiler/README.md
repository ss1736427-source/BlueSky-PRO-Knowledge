# Mission Compiler

EC-05 baseline: converts a validated Mission model into an executable mission package for a selected vehicle.

The implementation is a controlled contract/prototype. It does not implement autopilot transport, regulatory approval, or physical flight execution.

The Mission model is treated as input and is not mutated by compilation. Multi-vehicle compilation is a required subsequent increment before EC-05 can be considered complete.
